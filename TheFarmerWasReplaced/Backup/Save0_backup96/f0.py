clear() 

for i in range(get_world_size()):
	for j in range(get_world_size()):
		
		if Grounds == Grounds.Grassland:
			till()
		
		if En
		
		move(North)
		if j == get_world_size() - 1:
			move(East)
			
		
			
		
		